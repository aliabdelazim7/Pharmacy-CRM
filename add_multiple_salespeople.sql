-- ============================================================================
-- DIESEL Barbershop — دعم أكثر من كابتن (مسؤول) على الفاتورة الواحدة
-- شغّله مرة واحدة في: Supabase → SQL Editor → New query → Run
-- ============================================================================
-- عمود يخزّن قائمة الكباتن المنفّذين لكل فاتورة: [{ "id": "...", "name": "..." }]
-- العمولة/المبيعات تُقسَّم بينهم بالتساوي في تقارير الموظفين.
-- الأعمدة القديمة (salesperson_id / salesperson_name) تظل تُملأ بأول كابتن للتوافق.
-- دالة الفاتورة الإلكترونية get_public_invoice ترجع العمود تلقائياً (to_jsonb) — لا حاجة لتعديلها.
-- ============================================================================
alter table orders add column if not exists salespeople jsonb not null default '[]'::jsonb;
